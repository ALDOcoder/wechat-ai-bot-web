# 📚 RAG 知识库使用指南

> 适用范围：本地知识助手 Web 面板（wechat-ai-bot-web）
> 一句话理解：RAG = 提问时自动检索你的本地 Obsidian 笔记，让 AI 基于「你自己的笔记」回答，而不是凭通用知识瞎编。

---

## 一、日常使用（30 秒上手）

### 1. 确认检索开关已打开

输入舱左下角的 **「📄 知识库检索」胶囊**：

| 状态 | 含义 |
| --- | --- |
| 🔵 点亮（默认） | 提问时先检索本地笔记，再让 AI 结合笔记内容回答 |
| ⚪ 熄灭 | 普通闲聊模式，完全不碰笔记库 |

### 2. 像聊天一样提问

推荐这样问（指向笔记内容的问题效果最好）：

- 「我笔记里关于 **Spring 事务传播** 的要点有哪些？」
- 「总结一下 **30-Diary** 里最近一周记录的想法」
- 「根据我的笔记，**XX 项目**进展到哪一步了？」

### 3. 看标签确认命中

AI 回复气泡尾部的小标签就是「回答依据」：

| 标签 | 含义 |
| --- | --- |
| 📄 **RAG** | 本次回答检索到了你的笔记，内容有出处 |
| ⚡ DeepSeek / GLM-4.7 / GLM-4 | 本次回答**实际**由哪个模型生成（费用参考，以响应为准） |
| 已自动切换 | 你选的那档不可用或被限流，后端已换档一次（前一个标签才是真正生成者） |

> 💡 如果回答没带 📄 RAG 标签，说明这次没检索到相关笔记（或开关没开），AI 是用通用知识回答的。

---

## 二、知识库管理台

点击 **侧栏底部 →「🗂️ 知识库管理」** 打开。管理面有密钥门禁：首次要输入 `RAG_VAULT_KEY` 换临时令牌（存 `sessionStorage`，30 分钟滑动过期，后端重启即失效）。卡片顶部是索引状态条，下面分 **📁 文件 / 📐 规则** 两个页签。

### 1. 索引状态（只读）

```
✅ 检索已启用 · 27 文件 · 92 块 · 📚 C:\...\webflow
```

- **启用/停用**：整个检索功能的总开关（对应后端配置）
- **文件数 / 分块数**：当前**真正进了索引**的笔记规模，每次规则/索引变更后自动刷新（注意它远小于文件树里的文件总数——未解锁保护目录和排除规则命中的都不算）
- **库路径**：正在检索的 Obsidian 仓库位置
- 若出现红字「索引异常：...」，把内容反馈给后端排查

### 2. 「文件」页签：文件树与索引开关

**先理解一件事**：持密钥 = 库内 `.md` 文件名**全部可见**；「可见」和「能被 AI 搜到」是两层，后者才由索引开关决定。安全基线（见下）是唯一在任何接口都看不见的。

- 每个文件带状态点：🟢 可检索 · 🟡 未索引（属于未解锁保护目录）· ⚪ 已排除（命中自定义规则或 `_index`/moc 导航文件）
- 组头显示「N 可检索 · M 未检索」；保护目录单独成组，组内文件**照样逐条列出**
- **加入索引**：把该目录开放给会话检索（会真真切切被 AI 读到），所以点了会先弹二次确认；**移出索引**随时可撤回
- **＋ 新建笔记**：可写库内任意目录。写进未解锁目录时落盘会成功，但界面会提示「已落盘、AI 检索不到」——这不是 bug，要能问出来就去点该目录的「加入索引」

### 3. 基线规则（不可移除）

实测 `.mimocode`、`node_modules`、`.obsidian` 这三条是**安全底线**，永远不参与检索、界面上只能看不能删，保证系统目录与插件目录不会被泄露给 AI。

> ⚠️ `40-Life` **不在基线里**——它是「保护目录」：默认不进索引，但可以被解锁（见上）。想彻底钉死某目录，用下面的自定义规则。

### 4. 自定义规则（核心功能）

控制「除基线外，还有哪些目录/关键词不允许被检索」。

**➕ 新增规则**

1. 第一个输入框填**路径关键词**（子串匹配、忽略大小写），如：`私人目录`、`30-Diary`、`50-Resources/Templates`
2. 第二个输入框填备注（可选，方便以后想起这条规则是干嘛的）
3. 点「＋ 添加」→ **立即生效并自动重建索引**，不用手动刷新

**⏸️ 停用规则**：关掉规则行右侧的开关 → 该规则立即失效，对应文件**重新进入检索范围**（行内容会划线显示表示已停用）

**🗑️ 删除规则**：点行尾 ✕ → 行内出现「确认删除？」→ 再点红色「删除」才真正删除（⚠️ 删了 = 对应目录重新可被检索，请确认它不含隐私）

**状态区的文件数/分块数会跟着每次新增/停用/删除自动变化**——比如排除了一个大目录后数字变小，这就是索引重建完成的信号。

---

## 三、匹配规则详解（避免踩坑）

**实际排除范围 = 基线规则 ∪ 启用中的自定义规则**（并集）

| 规则 | 说明 |
| --- | --- |
| 匹配方式 | **相对路径子串包含**、忽略大小写 |
| 粒度建议 | 写得越具体越精准：`Templates` 会挡住所有含该词的路径；只想挡模板目录就写 `50-Resources/Templates` |
| 重复添加 | 后端会拒绝并提示「规则已存在：xxx」 |
| 长度限制 | 关键词与备注均不超过 200 字 |

**✅ 推荐写法**：`20-Projects/某项目/私有` （精确挡住某个子目录）
**❌ 容易误伤**：`tmp`（会挡住所有路径里带 tmp 的文件）、单个常见词

---

## 四、典型场景配方

| 场景 | 操作 |
| --- | --- |
| 🙈 保护隐私日记 | 添加规则 `30-Diary`，备注「私人日记勿检索」 |
| 🤖 屏蔽 AI 自动写的笔记 | Agent 写笔记功能输出到 `90-Agent` 目录，不想让它被检索就添加 `90-Agent` |
| ⏸️ 临时放开某目录 | 找到对应规则，关掉开关即可（不用删除，随时再打开） |
| 🔍 验证某关键词会挡住什么 | 浏览器直接访问 `http://127.0.0.1:8080/api/rag/search?q=关键词` 可调试检索结果 |

---

## 五、边界与注意事项（如实说明）

1. **排除只作用于「检索层」**：历史消息流水（message_log）与已有对话记忆不受影响——之前聊过的内容仍可能出现在对话上下文里，换库/加规则不会让 AI「忘掉」已聊内容（用「清空记忆」按钮重置上下文）。
2. **新笔记的收录时机**：笔记写入仓库后，会在下次索引重建时纳入；自定义规则变更、新增都会触发重建。库很大时重建需要短暂等待，期间文件/分块数会跳动。
3. **上游限流**：遇到「抱歉，AI 服务暂时不可用」多数是智谱高峰期限流（429），把模型切到 **GLM-4（免费备选）** 通常能继续用；这不影响 RAG 本身。
4. **排除/未索引只挡检索层，不挡落盘**：AI 或你本人往未解锁保护目录写笔记会成功，只是问不到；反过来，「加入索引」一步就把这些笔记开放给了会话检索——这是整个管理台里唯一真正改变隐私面的按钮，已有二次确认。
5. **访问面**：后端已只绑 `127.0.0.1:8080`（局域网设备直连 8080 读不到消息原文），手机走 `http://<本机IP>:5173` 由 Vite 代理转发仍可用。聊天/会话接口本身**没有鉴权**，`note/confirm`、`note/reject` 按设计免令牌（靠 128 bit 随机 `pendingId` 防伪造）；管理面有令牌 + 连错 5 次锁 10 分钟。不要把 8080 暴露到公网，也不要在前端把令牌写进 `localStorage` / URL / 日志。

---

## 六、常见问题 FAQ

**Q1：开了检索，但回答一直不带 📄 RAG 标签？**
按顺序检查：① 输入舱「知识库检索」胶囊是否点亮；② 问题是否太泛（「你好」这类闲聊不会命中笔记）；③ 设置卡片里索引是否正常、文件数是否 > 0；④ 想找的内容是否恰好被某条规则排除了。

**Q2：怎么确认某条笔记「能不能被搜到」？**
访问调试接口 `http://127.0.0.1:8080/api/rag/search?q=笔记里的关键词`，返回命中分块即为可检索。

**Q3：添加规则后文件数没变？**
说明该关键词没有匹配到任何已索引文件的路径（子串不命中），检查拼写和目录层级。

**Q4：能彻底「忘掉」某次对话吗？**
点顶栏「清空记忆」重置当前会话上下文；历史流水在后端数据库中，需到后端清理。

---

## 七、附：底层接口（进阶/调试用）

| 方法 | 路径 | 用途 |
| --- | --- | --- |
| `GET` | `/api/rag/status` | 索引状态（启用/文件数/分块数/库路径/最近错误） |
| `GET` | `/api/rag/patterns` | 规则列表（basePatterns 基线 + patterns 自定义） |
| `POST` | `/api/rag/patterns` | 新增规则 `{ "pattern": "...", "remark": "..." }` |
| `PUT` | `/api/rag/patterns/{id}` | 启停/改备注，如 `{ "enabled": false }` |
| `DELETE` | `/api/rag/patterns/{id}` | 删除规则 |
| `POST` | `/api/rag/refresh` | 手动重建索引（一般用不到，增删改已自动重建） |
| `GET` | `/api/rag/search?q=` | 调试检索，直接看命中结果 |

> 增删改的响应体里都携带重建后的 `files` / `chunks`，前端卡片据此实时刷新状态区。
