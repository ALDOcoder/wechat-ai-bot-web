# 开发总结 · 本地知识助手（wechat-ai-bot-web）

> 本文档由 AI 编程助手（ZCode）生成，回顾本项目从创建到当前的完整演进过程，重点是本轮会话中完成的全部工作。
> 项目详细介绍见 [项目介绍.md](项目介绍.md)，RAG 功能的日常使用见 [RAG使用指南.md](RAG使用指南.md)。

---

## 一、项目是什么

**本地知识助手**：一个基于 Vue 3 + Vite 的 AI 聊天面板（前端仓库），为 Java 后端 `wechat-ai-bot` 提供浏览器端交互。核心能力：

- 类 DeepSeek 官网的对话界面：多会话管理（按时间分组）、模型下拉切换、知识库检索开关
- RAG 问答：提问时检索本地 Obsidian 笔记，回答带来源标签
- **知识库管理台**（密钥门禁）：文件树浏览、受保护目录加入/移出索引、排除规则管理、手动建笔记
- **AI 写入确认**：AI 请求写笔记时弹确认卡片，人工批准才落盘
- 三模型通道：GLM-4.7（免费默认）/ GLM-4（免费备选）/ DeepSeek（付费需授权）
- 移动端完整适配（抽屉侧栏、安全区）

前端保持「零 UI 框架依赖」：运行时依赖只有 `vue` 一个，样式纯手写 CSS（设计令牌 + 组件类）。

---

## 二、本轮会话做了什么（按时间线）

### 1. 项目介绍文档
通读全部源码后输出 [项目介绍.md](项目介绍.md)：架构图、核心功能、后端 API 依赖清单、快速开始、设计说明。

### 2. 样式评价 → 全新视觉重构（最大的改动）
- 先对原样式做了专业评价：指出颜色硬编码 7 处、完全无响应式、视觉偏素等短板
- 经两轮方向确认（浅色大胆 → 用户指定 DeepSeek 官网风格），**全量重写 `styles.css`（183 行 → 962 行）**：
  - 建立 **CSS 设计令牌体系**（`:root` 变量：主色/灰阶/圆角/阴影/缓动），换主题只改变量
  - DeepSeek 式布局：纯白全屏 + 浅灰侧栏 + 蓝主色 `#4d6bfe` + 大圆角输入舱
  - 侧栏：会话按「今天 / 7 天内 / 30 天内 / 更早」分组，「开启新对话」胶囊按钮
  - 模型切换从顶栏迁入**输入舱内的下拉选择列表**（默认 GLM-4.7，带说明文案与选中勾）
  - 「知识库检索」改为输入舱内的胶囊开关
  - **移动端适配**：≤768px 侧栏变抽屉（汉堡按钮 + 遮罩）、顶栏压缩、气泡放宽 86%、iPhone 底部安全区
  - 细节：消息进场动画、打字三点、状态灯呼吸、自定义细滚动条、`:focus-visible`、`prefers-reduced-motion`

### 3. 接入第三个模型 GLM-4-Flash
按后端接口文档（`20260905_glm4flash-api.md`）落地：模型下拉加 GLM-4 项、会话偏好识别三值、回复气泡新增青色 GLM-4 标签。

### 4. RAG 排除规则管理卡片
按后端接口文档（`20260906_rag-patterns-api.md`）实现：索引状态区（启用/文件数/分块数/库路径）、只读基线规则 chips、自定义规则**新增/启停/删除**（删除二次确认），增删改响应自带的最新索引数直接刷新状态区，后端校验错误（如规则重复）直接回显。全流程浏览器实测通过。

### 5. 局域网访问支持
`vite.config.js` 增加 `host: true`（默认只监听 localhost，局域网设备访问不到），并清理了端口占用问题。之后同一 WiFi 下的手机/其他电脑可通过 `http://<本机IP>:5173` 访问。

### 6. 排查「会话设置接口 404」
应要求诊断 `GET /api/conversations/:id/setting` 的 404：确认**是设计行为**（无记录返回 404，前端回落默认模型）；顺带发现一个真实契约错位——后端 PUT 校验用内部模型 ID（`glm-4.7flash`）而前端按文档发别名（`zhipu`），导致**模型偏好持久化静默失败**（每次刷新重置为默认模型）。经确认当前暂不修复，已记录在案。

### 7. 给后端写需求开发书（前端视角）
- 先写了会话级密钥解锁方案（`20260906_rag-40life-unlock-api.md`），用户调整需求后**作废重写**
- 最终版 `20260906_vault-filetree-write-confirm-api.md`（v2）：**密钥门禁**（密钥换临时令牌，无密钥看不到文件树）+ **文件树三态** + **受保护目录加入/移出索引** + **手动新建笔记** + **AI 写入两阶段确认**，含安全规则（XFF 防伪造、防爆破、路径越界校验）、9 项待确认点和联调验收清单

### 8. Vault 前端对接（后端完成后）
按后端完成报告全量落地，本次新增约 700 行：

| 功能 | 实现要点 |
| --- | --- |
| 🔐 密钥锁屏 | 打开管理台探测 `/api/rag/status`，401 显示锁屏（不泄露任何索引数字）；密钥换令牌存 `sessionStorage`，所有管理请求自动带 `X-Vault-Token`；令牌失效自动回锁屏 |
| 📁 文件树 | 按顶层目录分组，三态标记（🟢可检索 / ⚪已排除+来源规则 / 🔒受保护折叠节点），40-Life 子文件在加入索引前完全不可见 |
| 🔓 保护目录 | 「加入索引 / 移出索引」一键切换，响应实时刷新索引数；移出按钮位置记忆在 localStorage |
| ✍️ 手动建笔记 | 路径 + Markdown 内容，后端校验错误（已存在 409 / 越界 400）直接回显 |
| 📝 AI 写入确认 | 回复带 `pendingNotes` 时渲染确认卡片（路径 + 内容预览 + 确认/拒绝），豁免令牌、一次性防抖、用响应实际路径回显 |

**实测验证**：锁屏无数字泄露 ✓ 错误密钥提示「密钥不正确」✓ 豁免接口 404/幂等 ✓；并测出一个有意思的真实场景——**AI 文字回复碰上智谱限流失败，但笔记草稿照常生成**，确认卡片正常渲染，「确认写入」返回 `✅ 已写入 90-Agent/知识库管理前端对接完成.md`，两阶段写入闭环打通。

### 9. 密钥运维脚本
新写 `Spring-AI/reset-vault-key.ps1`：自动生成（或指定）管理密钥 → 写入用户环境变量 → 安全停止旧后端（只杀 java 进程，端口被其他程序占用则拒绝操作）→ 新窗口重启 → 健康检查 → 验证门禁 401 生效。解决了「密钥只在启动进程内存里、注册表查不到」的找回难题。

---

## 三、提交记录（本轮会话产生 4 个提交）

| 提交 | 内容 |
| --- | --- |
| `78abbf9` | DeepSeek 官网风格 UI 重构 + 移动端适配，模型改输入舱内下拉（默认 GLM-4.7）并接入 GLM-4-Flash 备用模型 |
| `bc07b8d` | dev 配置：Vite 监听 0.0.0.0，支持同一局域网设备访问 |
| `12911eb` | RAG 知识库设置卡片（索引状态展示 + 排除规则增删改启停，变更实时刷新索引数） |
| `44fc362` | 对接 Vault 门禁与文件树——密钥锁屏、知识库管理双页签、保护目录加入/移出索引、手动建文、AI 写入确认卡片 |

配套产出文档：`项目介绍.md`、`RAG使用指南.md`、后端需求书两份（`Spring-AI/agent/zhipu/` 下）、密钥运维脚本 `reset-vault-key.ps1`。

---

## 四、遗留事项

| 事项 | 状态 |
| --- | --- |
| 模型偏好持久化：后端 PUT 校验用内部模型 ID，前端发别名，导致偏好存不进去（刷新重置默认模型） | 已诊断，**用户决定暂不修**；聊天不受影响（provider 随每条消息显式传递） |
| 解锁后的管理面（文件树渲染、保护目录加入/移出、手动建文、规则页签） | 代码已就绪，需持有 `RAG_VAULT_KEY` 的用户实测验收 |
| `RAG使用指南.md` 尚未提交入库 | 待定 |
| 聊天偶发「AI 服务暂时不可用」 | 上游智谱限流（429），非本项目问题；可切 GLM-4 备用模型缓解 |
| 后端 mock 完成报告提到的 `_index.md`/moc 导航文件排除 | 后端已实现（树中标记 `rule="导航文件(_index/moc)"`），前端已支持展示 |
