# 开发规格说明书 · wechat-ai-bot-web

> **本文的定位**：README 讲「怎么跑起来」，`项目介绍.md` 讲「有什么功能」，本文讲**「怎么改、为什么长成这样、改完凭什么算合格」**。
> 面向后来人（接手的人类开发或 AI 协作者）。改动本仓库前请先读完 §1、§7、§9、§10。
>
> 文中所有「现状」数字由 `npm run check:report` 现场实测得出，不靠记忆；口径见 §7.4。

---

## 0. 开工速览

| 事实 | 值 | 证据 |
| --- | --- | --- |
| 运行时依赖 | 只有 `vue` 一个包 | `package.json:11-13` |
| 前端代码总量 | `App.vue` 1091 行 + `styles.css` 1650 行 + `treeGroups.js` 69 行 + `main.js` 5 行 | `npm run check:report` |
| 组件数 | **1 个**（`App.vue`），无任何子组件 | `git ls-files` 只有 3 个 src 文件 |
| script 段构成 | 36 个 `ref` + 4 个 `computed` + 34 个函数 + 19 个 `catch` | `src/App.vue`（`<template>` 之前的整段） |
| 后端调用点 | 22 处：14 处直接 `fetch(` + 8 处经 `vaultFetch()` 包装；`'/api/...'` 字面量 21 处，**全部散在 App.vue** | `src/App.vue:71,90,112,122,139,176,217,259,282,302,361,391,416,429,447,462,485,508,536,571,593,623` |
| 后端 | Spring Boot :8080，仓库 `ALDOcoder/wechat-ai-bot`，本地代码在 `C:\Users\ChelseaFC\Desktop\12Java\study\Spring-AI` | `vite.config.js:9-13` |
| 已知结构性重复 | 输入舱模板 **63 行复制了 2 遍** | `src/App.vue:764` 与 `src/App.vue:851`（校验器按内容哈希识别，移动行号不影响） |
| 已知色值泄漏 | `:root` 之外硬编码颜色 **46 处** | `src/styles.css`（`--report` 可定位） |

> **基线注记**：2026-09-21 因「文件树全可见 + `indexed` 语义」这轮契约变更，`App.vue` 基线由 1066 放到 **1091**、`styles.css` 由 1617 放到 **1650**（新语义带来的净增 UI：二次确认行、未索引标记、写入结果提示）。这笔账由 **P1** 偿还；R3 色值同期由 48 降到 46（新样式一律走 `var()`）。

**一句话结论**：功能是对的、跑通的、有实测记录；结构已经到「下一个改的人会在错的地方改」的临界点，需要按 §8 分阶段拆解，但**不要一次性重写**。

---

## 1. 一个巨型 SFC 到底卡在哪

`App.vue` 现在同时承担 6 个互不相干的领域，它们共享一份 `ref` 池，彼此能互相摸到状态：

| # | 领域 | 代码位置 | 为什么会互相拖累 |
| --- | --- | --- | --- |
| 1 | 会话生命周期（列表/分组/新建/删除/切选中） | `groupedSessions` / `loadSessions` / `selectSession` / `deleteSession` / `newSession` | `activeId` 被 2/3/4/5 全都读 |
| 2 | 模型选择与偏好持久化 | `MODELS` / `loadModelSetting` / `persistModelSetting` / `selectModel` | `sending` 锁与聊天流程共用 |
| 3 | 消息收发与渲染 | `send` / `confirmNote` / `rejectNote` / `clearMemory` + 气泡模板 | 直接改 `sessions`（`loadSessions()`）|
| 4 | RAG 排除规则 CRUD | `openRagCard` / `loadPatterns` / `addPattern` / `togglePattern` / `deletePattern` | 与 5 共用 `ragError`/`ragLoading` |
| 5 | Vault 门禁 + 文件树 + 建文 | `vaultToken`/`unlockVault`/`lockVault`/`vaultFetch` + `loadTree`/`protectAdd`/`protectRemove`/`createNote` + 弹窗模板；分组算法已外移到 `src/treeGroups.js` | 弹窗模板和聊天模板塞在同一 `<template>` 里 |
| 6 | 布局与移动端抽屉 | 侧栏 / `stage` 欢迎页 / `panel` 会话页三段模板 | 与 3 的模板共用 `ref="inputBox"` |

**具体代价（不是「不优雅」，是可核算的）**：

1. **63 行模板 × 2 份**（`App.vue:764` 会话页输入舱、`App.vue:851` 欢迎页输入舱）。改发送逻辑或加一个模型选项要同步两处，漏一处就是一个只在某个页面出现的 bug——这是最典型的「同一段需求有两个真相」。
2. **`ref="inputBox"` 同名两处**（`App.vue:762`、`App.vue:849`）。目前**能正常工作**，因为外层 `v-if="messages.length > 0"` / `v-else` 互斥（`App.vue:708`、`App.vue:827`）。这个前提是隐式的：一旦有人给欢迎页也加个「最近消息」区块破了互斥，`autoResize()`（`App.vue:62-67`）就会作用在错误的 DOM 上，且不报错。
3. **一个全局 `sending` 布尔承担 6 种并发语义**（`App.vue:5`）。「AI 正在回话」和「正在拉文件树」用的是同一个开关，所以 `:disabled="sending"`（`App.vue:674,717,776`）会在你打开管理台刷新索引时把聊天输入也一起锁死。
4. **状态无法复用**：想给管理台加一个「索引重建进度轮询」，只能在同一个文件里再加 3 个 `ref`，行数继续涨；想写单测，必须挂载整个 App。
5. **`styles.css` 1650 行 / 238 个规则块**，靠 `/* ---------- 侧栏 ---------- */` 这类注释分节（共 15 节）。组件与样式没有物理就近关系：改 `.composer`（`styles.css:709`）得从 `ChatComposer` 跳出去翻 700 行。

**但也必须说清它现在的优点，别弄丢**：零依赖、无构建配置魔法、一眼能读到底、新人 10 分钟能改一行上线。§8 的拆解以「不牺牲这三点」为硬约束。

---

## 2. 目标分层与依赖方向

```
视图层   src/App.vue（外壳：布局 + 抽屉 + 弹窗挂载） + components/**
                                ↓ 只允许调用
编排层   composables/**（状态 + 动作 + 竞态处理）
                                ↓ 只允许调用
契约层   src/api/**（唯一的 fetch、唯一的 '/api/' 字面量、唯一的错误归一化）
                                ↓ 只读
常量层   src/config/**（MODELS 值域、存储键名、断点、超时）
```

**硬规则（§7 的校验器逐条对应）**：

- 视图层不得出现 `fetch(` 或 `'/api/`；不得直接 `import` 另一层的 `ref`。
- 依赖只能向下。`api/` 里禁止出现任何 Vue 响应式 API。
- 组件之间不共享可变状态；共享一律经 `composables/` 导出的单例 store。
- `config/constants.js` 是**人工维护的唯一真相**（模型值域、存储键、错误文案），机器产物不得写入该文件。

---

## 3. 目录与命名规范

目标结构（新增文件必须落进对应目录，不许再造根级巨型文件）：

```
src/
├── main.js                  # 挂载（≤10 行）
├── App.vue                  # 外壳：侧栏 + 主面板 + VaultConsole 弹窗（目标 ≤120 行）
├── api/                     # 契约层
│   ├── http.js              # request()：JSON/文本解析、ApiError、超时、令牌注入钩子
│   ├── chat.js              # reply()、clearMemory()
│   ├── sessions.js          # list()、messages()、remove()
│   ├── settings.js          # getSetting()、putSetting()（含前后端模型 ID 映射）
│   ├── rag.js               # status()、patterns CRUD
│   └── vault.js             # session()、tree()、protect add/remove、note、confirm/reject
├── composables/             # 编排层，每个 ≤120 行
│   ├── useSessions.js       # 列表 + 时间分组 + 选中
│   ├── useChat.js           # messages + send + 竞态序号
│   ├── useModel.js          # 模型选择与偏好
│   ├── useVaultAuth.js      # 令牌生命周期（401 → 回锁屏）
│   └── useRagRules.js       # 基线 + 自定义规则 CRUD
├── components/              # 纯展示 + 事件上抛，每个 ≤200 行
│   ├── ChatComposer.vue     # ★ 输入舱唯一实现，两处复用（消掉现有 63 行重复）
│   ├── ModelPicker.vue      # 模型下拉
│   ├── MessageBubble.vue    # 一条气泡 + 标签 + 写入确认卡片
│   ├── NoteConfirmCard.vue
│   ├── SessionSidebar.vue
│   ├── WelcomeStage.vue
│   ├── ServerStatusDot.vue
│   └── vault/
│       ├── VaultConsole.vue # 管理台容器（锁屏/状态条/页签）
│       ├── LockScreen.vue
│       ├── FileTree.vue
│       └── RuleList.vue
├── config/
│   ├── constants.js         # MODELS / MODEL_ID_MAP / STORAGE_KEYS / BREAKPOINT / TEXTS
│   └── storage.js           # 唯一允许碰 localStorage/sessionStorage 的文件
└── styles/
    ├── tokens.css           # ★ 唯一允许出现颜色/圆角/阴影字面量的文件
    ├── base.css             # 骨架、滚动条、选区、焦点、动画
    └── components/<name>.css # 与组件同名，按需引入
```

**现状**：目标结构里只有 `src/treeGroups.js`（文件树分组算法，纯函数无 Vue 依赖）已经落地，是 2026-09-21 那轮契约改造顺带外移的；`api/`、`composables/`、`components/`、`config/`、`styles/` 仍待 §8 的 P0-P4。

| 对象 | 规则 | 例 |
| --- | --- | --- |
| 组件文件 | PascalCase，与唯一根 class 同名 | `ChatComposer.vue` / `.composer` |
| composable | `useXxx.js`，导出 `export function useXxx()` | `useVaultAuth` |
| api 模块 | 以**资源**命名，不用动词 | `sessions.js` 而非 `getSessions.js` |
| api 函数 | 动词原形，返回**已解析的业务数据**，不返回 `Response` | `listSessions()` |
| store 状态 | `state` 内的 `ref` 只读导出，写操作只经 action | `const { messages } = useChat()` |
| CSS 类 | 现有语义类名延续（`.bubble` `.composer` `.tree-file`），不引入工具类（`.mt-2` 之类） |
| 注释 | 只在「为什么」非显然处写一行；复述代码的注释删除 |

---

## 4. 契约层规范（单一事实源）

### 4.1 端点清单（当前 21 个调用点归并而来）

| 模块 | 函数 | 方法 + 路径 | 成功 | 失败语义与前端义务 |
| --- | --- | --- | --- | --- |
| — | `health()` | `GET /api/health` | `{status:'ok'}` | 网络异常 → 状态灯转离线 |
| sessions | `listSessions()` | `GET /api/sessions` | `[{conversationId,title,lastTime,msgCount}]` | 非 2xx → 空列表，**不弹错** |
| sessions | `listMessages(id)` | `GET /api/sessions/web:<id>/messages` | `[{direction,content,provider,useRag}]` | `direction` 只认 `RECEIVED`(用户) / `SENT`(AI) |
| sessions | `deleteSession(id)` | `DELETE /api/sessions/web:<id>` | — | 失败**仅本地移除**（现状即如此，`App.vue:177-179`） |
| settings | `getSetting(id)` | `GET /api/conversations/web:<id>/setting` | `{preferredModel}` | **404 = 该会话没存过偏好**（设计行为）→ 回落默认，别弹错；实测 2026-09-21 值域已对齐前端别名 |
| settings | `putSetting(id,payload)` | `PUT /api/conversations/web:<id>/setting` | `{conversationId,scene,preferredModel,allowDeepseek}` | 值域 `zhipu|glm4flash|deepseek`，非法值 400 纯文本；前端现状**不校验返回值**（写失败仍静默） |
| chat | `reply(payload)` | `POST /api/reply` | `{reply,provider,ragUsed,pendingNotes}` | 网络异常 → 气泡内中文错误指引（`App.vue:243-247`） |
| rag | `ragStatus()` | `GET /api/rag/status` | `{enabled,vaultPath,files,chunks,lastError}` | **401 = 未解锁/令牌过期 → 回锁屏**；锁屏态不得显示任何索引数字。`files` 只算**真正进索引**的，必然小于文件树里的文件总数 |
| rag | `listPatterns()` | `GET /api/rag/patterns` | `{basePatterns,patterns}` | 401 同上。基线实测 3 条：`.mimocode` / `node_modules` / `.obsidian`（**不含 `40-Life`**，它是可解锁的保护目录） |
| rag | `addPattern()` | `POST /api/rag/patterns` | `{row,files,chunks}` | 400 响应体是**纯文本**，直接回显 |
| rag | `togglePattern()` | `PUT /api/rag/patterns/:id` | `{row,...}` | 404 → 重新 `listPatterns()` |
| rag | `deletePattern()` | `DELETE /api/rag/patterns/:id` | `{files,chunks}` | 404 → 本地直接移除 |
| vault | `openSession()` | `POST /api/vault/session` `{key}` | `{token}` | 401/429 响应体纯文本回显（含防爆破提示：连错 5 次锁 10 分钟） |
| vault | `tree()` | `GET /api/vault/tree` | `{root,nodes}` | 持令牌 = **全库 `.md` 可见**，基线永不出现。节点只有两类 `status`：文件（`indexed`/`excluded` + 可选 `rule` 纯文本原因）与目录级折叠节点 `{path:'MyVault/40-Life/',status:'protected',dir:'40-Life',fileCount}`；折叠节点**仅在未加入索引时下发**，加入后消失且其下文件转 `indexed`。节点顺序不保证，前端自己排序 |
| vault | `protectAdd/Remove()` | `POST /api/vault/protect/add\|remove` `{path}` | `{files,chunks}` | `path` 传配置名（`40-Life`）或真实前缀（`MyVault/40-Life`）**都收**；400 = 不是保护目录 → 清理本地记忆位 |
| vault | `createNote()` | `POST /api/vault/note` `{path,content}` | `{path,files,chunks,`**`indexed`**`}` | `path` 以**返回值**为准（同名会加 `-2`）；`indexed:false` = 落盘成功但检索不到，必须提示。400 基线路径/越界/非 .md/超 20000 字、409 已存在，均为纯文本 |
| vault | `confirmNote/rejectNote()` | `POST /api/vault/note/confirm\|reject` `{pendingId}` | 同上（含 `indexed`） | **豁免令牌**，一次性；`pendingId` = `pn_` + 32 位 hex（别按旧 12 位长度截断）；404 = `确认已过期，请让 AI 重新生成` |

### 4.2 `http.js` 必须承担的 5 件事

```js
// 约定，不是实现草案的强制签名
request(path, { method, body, auth = false, text = false })
// 1. 拼 '/api' 前缀 —— 组件层不再出现任何路径字面量
// 2. 非 2xx 抛 ApiError { status, text }，由编排层决定「回显 / 静默 / 回锁屏」
// 3. auth=true 时注入 X-Vault-Token；收到 401 统一触发 onLocked 回调
// 4. 后端错误体是 text/plain 与 application/json 混用的，Accept 不发 json，按 Content-Type 分支
// 5. 默认 15s 超时（AbortController）；/api/reply 单独放宽到 60s
```

**禁止**：在 `catch {}` 里静默改写状态而不留一行说明。全文件 19 个 `catch`，其中 6 处纯静默（`loadModelSetting`、`persistModelSetting`、`loadSessions`、`loadMessages`、`loadPatterns`、`loadTree` 各自吞掉异常后只留一行注释）。静默必须是**决定**，要写清「为什么可以不告诉用户」——例如 `App.vue:177-179` 删会话失败时静默是对的，因为会话列表随后会被 `loadSessions()` 纠正。

### 4.3 持久化真相表（新增键必须登记，校验器 R5 会拦）

| 数据 | 存在哪 | 权威源 | 失效条件 | 为什么 |
| --- | --- | --- | --- | --- |
| 会话列表 / 消息 | 后端 `message_log` | 后端 | 后端删除 | 换设备要能续上 |
| `preferredModel` | 后端 setting | 后端 | 会话删除即清除 | 只用于**回填下拉框**；发消息仍以每条携带的 `provider` 为准（D5） |
| Vault 令牌 | `sessionStorage.vaultToken` | 后端内存 | 关标签页 / 30 分钟滑动过期 / 后端重启 | 令牌只在这台设备的这个标签页有意义 |
| `vaultProtectAdded` | `localStorage.vaultProtectAdded` | **仅本地记忆**（服务端无此查询） | 换设备/清缓存即丢 | 只用来决定「移出索引」按钮显示在哪，丢了不致错，见 §10-P4 |
| 当前选中会话 | 内存 | — | 刷新即回到 `sessions[0]` | 现状未持久化，属可改进项（§8-P2 顺带处理） |
| RAG 开关 | 内存（默认 `true`） | — | 刷新回默认 | 与 `vaultToken` 不同级，不必持久化 |

---

## 5. 状态与并发规范

1. **不引入 Pinia**。零依赖是本项目对外宣称的卖点（`项目介绍.md:167`），而 6 个 `ref` 池的规模用「模块级 `ref` + 导出单例 composable」完全够：

```js
// composables/useVaultAuth.js —— 模块级 ref 即单例 store，无需插件
import { ref } from 'vue'
const token = ref(sessionStorage.getItem('vaultToken') || '')
export function useVaultAuth() { /* 返回 state + actions */ }
```

2. **拆分 `sending`**。按用途给三个独立锁，禁止跨领域复用：
   - `chatStore.sending`：仅锁聊天收发（模型下拉、发送按钮）
   - `vaultStore.busy`：管理台规则增删改
   - `treeStore.loading`：文件树/建文
3. **竞态**：`switchSession()` 期间到达的旧响应必须丢弃。规范：每次请求带自增 `seq`，回填前校验 `seq === latestSeq`。现状靠 `sending` 挡切会话（`App.vue:165`），拆锁后必须换成 `seq`，否则会出现「A 会话的回复落到 B 会话气泡里」。
4. **乐观更新清单**（只允许这几处，且必须可回滚）：新建会话（`App.vue:154-162`）、删除会话本地先移除（`App.vue:180`）、规则新增后 push `row`（`App.vue:469`）。**消息气泡不做乐观**——AI 回复以服务端返回为准。
5. **组件不得直连 store 写入**：只 `emit`，由容器调 action。

---

## 6. 样式规范

1. **令牌唯一源**：颜色、圆角、阴影、缓动只允许写在 `styles/tokens.css`（现状 `styles.css:13-44` 是令牌段，共 20 个变量）。`:root` 之外现存 **46 处**硬编码色值（`#fff`、`#dc2626`、`#b45309` 等），是 P4 的收紧目标——新写样式一律用 `var(--…)`，校验器 R3 会拦住数量回升。
2. **主题变量命名**：`--c-<语义>`（`--c-primary` `--c-danger` `--c-text-3`）、`--r-<档位>`（`--r-sm/md/pill`）、`--sh-<档位>`。禁止 `--color-blue-500` 这种「色名而非语义名」的新变量。
3. **就近**：P4 之后 `components/Foo.vue` 的规则进 `styles/components/foo.css`，由 `main.js` 顺序引入（`tokens → base → 组件`）。
4. **响应式断点只有一个**：`768px`（`styles.css:1456` 起）。要加第二个断点，先在本节登记再写代码。
5. **可访问性不得回退**：`prefers-reduced-motion`（`styles.css:1608`）、`:focus-visible`、装饰性 SVG 上的 `aria-hidden="true"` 是既有约定。
6. **组件内不写 `<style>`**：本仓库样式全在外置 CSS，别开第二种存放处。

---

## 7. 验收规范

### 7.1 提交前必须绿

```bash
npm run check          # 退出码 0 才算合格（棘轮：只拦新增违规）
npm run check:report   # 看现状全量债务
npm run check:bump     # 债务真实减少后，人工收紧基线
```

### 7.2 校验器规则 ↔ 本文条款

| 规则 | 拦什么 | 条款 |
| --- | --- | --- |
| R1 体积 | 任一 src 文件行数超过基线；新文件 > 400 行 | §3 各层上限 |
| R2 契约 | `fetch(` / `'/api/` 出现在 `src/api/` 之外且计数上升 | §2、§4.1 |
| R3 令牌 | `:root` 之外色值字面量计数上升 | §6.1 |
| R4 重复 | ≥15 行的重复模板块新增，或既有块拷贝份数增加 | §1 代价 1 |
| R5 存储 | 出现未登记的 `localStorage`/`sessionStorage` 键 | §4.3 |
| R6 文档 | `README.md` / `项目介绍.md` 里的「N 行」与实测偏差过大 | §7.4 |

### 7.3 手工冒烟清单（每次涉及 §4/§5 的改动跑一遍，后端需在 8080）

1. 冷启动：状态灯转「服务在线」，侧栏拉到会话，首会话消息渲染正确。
2. 发一条消息：出现打字三点 → 回复带模型标签；关后端再发 → 气泡内中文错误指引而非白屏。
3. 切模型 → 刷新页面：**应恢复所选**（§10-P1 已实测修复）。若又回落到默认，先 `GET /api/conversations/web:<id>/setting` 看后端到底存了什么，再判前端。
4. 清空记忆 → 再问一句需要上文的话，确认 AI 不再引用前文。
5. 管理台：不带密钥打开 → 锁屏且**不出现任何文件/块数字**；错密钥 → 回显「密钥不正确」；对密钥 → 文件树按五态渲染（🟢 可检索 / 🟡 未索引+原因 / ⚪ 规则排除 / 🔵 导航文件 / 🔒 目录开关），组头「N 可检索 · M 未检索」，规则增删改后 `files/chunks` 变化。**验证「加入索引」只到二次确认行出现即点取消**（见 §10-P13）。
6. 令牌失效路径：重启后端 → 再点「刷新」→ 应自动回锁屏并提示「会话已过期」。
7. AI 写入卡片：带 `pendingNotes` 的回复 → 确认写入返回 `✅ 已写入 <path>`；重复点第二次 → 一次性失败文案。

### 7.4 口径（数字怎么算的）

- 行数 = 以 `\n` 切分、去掉文件末尾空行；CRLF 先归一成 LF。
- 参与 R6 的文档只有 `README.md` 与 `项目介绍.md`（对外说明，必须与代码同步）。`开发总结.md` 是**时间线快照**、`RAG使用指南.md` 是操作手册，二者记录的是「当时的事实」，不参与校验，也**不要**为了让数字对齐而去改写历史结论。
- R4 的重复块判定：忽略空行、行内空白归一后取 15 行滑窗哈希；每份拷贝计 1 个 start。
- **行号只是快照**：本文出现的 `App.vue:NNN` 是 2026-09-21 的实测位置，改动后必然漂移。定位请优先用同句给出的**符号名**（函数名 / computed 名 / CSS 选择器），校验器也一律按内容哈希而非行号比对。
- **纯函数模块要能脱离组件验证**：`src/treeGroups.js` 这类无 Vue 依赖的模块，用 `node -e "import(...)" + 后端真实数据` 即可核对分组结果（本轮实测：4 组、可检索合计 27，与 `/api/rag/status` 的 `files:27` 一致）。新增此类模块时沿用该做法。

---

## 8. 演进路线（每阶段独立可发布、零行为变更）

**原则**：一次只搬一类关注点；搬完 `npm run check` 绿 + §7.3 冒烟绿，就是一次可回滚的提交。**不要**在一个提交里同时改结构和改行为。

| 阶段 | 做什么 | 改动面 | 验收 | 回滚 |
| --- | --- | --- | --- | --- |
| **P0** 契约层 | 建 `src/api/*` + `config/constants.js`，把 21 处路径、14 处 `fetch` 全搬进去；`App.vue` 只调函数 | 新增 ~6 文件，`App.vue` 约 -120 行 | R2 归零（`src/api/` 之外 0 处）；冒烟 1-7 | 单提交 revert |
| **P1** 消重复 | 抽 `ChatComposer.vue` + `ModelPicker.vue`，两处调用同一组件；`ref="inputBox"` 随组件内聚 | `App.vue` -120 行左右 | **R4 基线清空** + `App.vue` 降回 1066 以下（本轮契约变更已把基线放到 1091，见 §0 注）；冒烟 2 | 单提交 revert |
| **P2** 拆编排 | `useSessions/useChat/useModel`，拆 `sending` 三锁 + 引入 `seq` 竞态保护；顺带把 `activeId` 存 `sessionStorage` | `App.vue` 只剩模板 | R1 上限按新结构重设；冒烟 2、3 | 拆成 2 个提交分别回滚 |
| **P3** 拆管理台 | `vault/VaultConsole.vue` 等 4 个组件 + `useVaultAuth/useRagRules`；令牌逻辑收进 `api/http.js` 钩子 | 弹窗整块搬出 `App.vue` | 冒烟 5、6、7 | 单提交 revert |
| **P4** 拆样式 | `styles/tokens.css` + `base.css` + `components/*.css`；把 46 处游离色值收敛进令牌 | 纯样式 | R3 归零；肉眼比对 5 个断点/页签 | 按 css 文件分 2-3 个提交 |

P0 之后**才**讨论功能新增（Markdown 渲染、SSE 流式）——否则又往同一个文件里堆。

---

## 9. 决策记录（为什么是现在这样，动之前先读）

| # | 决策 | 原因 | 代价 / 何时重评 |
| --- | --- | --- | --- |
| D1 | 零 UI 框架、零 Pinia、零 Axios，只用 `fetch` + Vue 响应式 | 面板规模小，装完依赖 <200KB；学习项目要能一眼读懂 | 表单/表格复杂起来会吃亏；届时优先引入组件库而非全家桶 |
| D2 | 会话 ID 前端生成 UUID，`'web:'` 前缀由前端拼（`App.vue:57-59`） | 后端按 `conversationId` 落库，前缀用来区分为微信端会话 | 前缀散落 4 处，P0 收进 `api/sessions.js` |
| D3 | Vault 令牌存 `sessionStorage` 而非内存 | 同标签页刷新免重输；关页即失效，符合 30 分钟滑动 + 后端重启失效的生命周期 | XSS 可读；当前消息正文走 `{{ }}` 插值不注入 HTML，风险可控——**引入 Markdown 渲染时必须重评这条** |
| D4 | 「已加入索引的保护目录」用 `localStorage` 记位置 | 后端 `tree` 接口不回传「你加过哪个」，前端只想知道该在哪一行显示「移出索引」按钮 | 换设备不显示按钮（服务端状态仍正确）；P3 前建议后端 `tree` 直接返回 `indexed:true`，届时删掉这个键 |
| D5 | `provider` 随每条消息显式传，不依赖偏好持久化 | 两条链解耦：偏好只决定**下拉框显示什么**，发出去的每条消息自带意图，后端才不会猜错。早期还顺带绕开了 §10-P1 的值域 bug（现已修） | 代价是 `getSetting` 与 `reply` 语义重叠；若将来做「跨端续上同一模型」，以 setting 为准、发消息时仍显式带上 |
| D6 | 无登录体系，后端按客户端 IP 区分会话 | 自用工具，接入成本最低 | 局域网多人共用出口 IP 会串会话；对外部署前必须换 token 方案 |
| D7 | 「清空记忆」复用 `POST /api/reply` 发关键词，而非独立端点 | 后端当时只有 `/api/reply` 一条入口 | 语义脆弱（关键词改了前后端要同步）、且在消息流里留一条假的「清空记忆」气泡；等后端给 `DELETE /api/conversations/:id/memory` |
| D8 | 开发期 `host: true` + `/api` 代理（`vite.config.js:6-13`） | 手机在同一 WiFi 可实测移动端适配 | 局域网内任何人可访问你的知识库管理台（只有 IP 隔离）；生产部署须走 Nginx 反代 + 收紧 |

---

## 10. 坑清单（都是实测过的，别重复踩）

- **P1 模型偏好持久化**：**已修复，别再照抄旧结论**。旧症状是前端发别名而后端按内部 ID（`glm-4.7flash`）校验 → 写不进去、刷新回默认（`开发总结.md:90` 记录过，当时决定暂不修）。2026-09-21 实测：`PUT preferredModel=deepseek` → 200，`GET` 回读一致；非法值 400 且文案为「preferredModel 只能是 zhipu / glm4flash / deepseek」；`DELETE /api/sessions/:id` 后 `GET` 回 404（探针数据不留残）。三层口径不变：**值域是后端常量**（改不了）、**偏好是后端每会话存储**（改了立即生效）、**`provider` 是每条消息的用户输入**（永远以它为准）。
- **P2 `GET /api/conversations/:id/setting` 返回 404 是设计行为**，不是后端挂了。前端必须回落到默认模型（`App.vue:72-74`），别加错误提示。
- **P3 状态灯不实时**：`checkHealth()` 只在 `onMounted` 跑一次（`App.vue:649`）。后端中途挂掉，界面仍显示「服务在线」。要么加轮询，要么把 `项目介绍.md:75` 的「实时」改掉。
- **P4 保护目录按钮位只在本地**：换设备后看不到「移出索引」，但服务端**已加入索引的状态是生效的**（`files` 数字能佐证）。这是 D4 的已知代价，不是 bug。
- **P5 上游 429 时会出现「文字失败、草稿照生」**：智谱限流时 `reply` 文案是错误提示，但 `pendingNotes` 仍返回、确认卡片仍渲染（`开发总结.md:66` 实测记录）。**别当 bug 修掉**——两阶段写入正是要独立于文案成功。
- **P6 后端重启 = 所有 Vault 令牌作废**：任何管理请求返回 401 → 必须回锁屏并清 `sessionStorage`（`App.vue:360-367` 已实现，重构时不能丢这段）。
- **P7 两处同名 `ref="inputBox"`**：只在 `v-if/v-else` 互斥下安全，见 §1 代价 2。P1 抽组件后自动消失。
- **P8 管理面错误体是纯文本**：400/401/409/429 直接 `res.text()` 回显（`App.vue:437,475,498`）。别写 `res.json()`，会抛解析异常并吞掉用户该看到的提示。
- **P9 本机是 Git Bash + GBK 控制台**：PowerShell 直接打印中文会乱码。写脚本一律用 Node（`scripts/check.mjs` 就是为此），别用 `echo`/`sed` 处理中文文件内容。
- **P10 文档与代码会漂移**：本文交付前实测发现 `项目介绍.md` 曾写「App.vue 约 350 行」（实际已 1066），`README.md:11` 的功能列表停在 3 个特性，Vault 管理台/多会话/模型切换都没提。R6 规则就是为这个装的。
- **P11 文件树不能按 `segs[0]` 分组**：库根下真实路径常带 `MyVault/` 这类包装层，按第一段分组会把 85 个文件塞进一个叫 `MyVault` 的组（实测 91 节点 → `MyVault:85 / 90-Agent:5 / 30-Knowledge:1`）。正确做法：保护目录（折叠节点 `path` 或本地记忆的 `protectAdded`）**单独成组**，其余按第一段分。实现见 `src/treeGroups.js` 的 `buildTreeGroups` / `collectPrefixes`。
- **P12 保护目录的真实前缀 ≠ 配置名**：折叠节点 `path` 是 `MyVault/40-Life/`，`dir` 才是 `40-Life`；老 `localStorage` 里存的可能是其中任一种。`isSamePrefix()` + 段序列匹配就是为了两种都兼容，**别退回 `protectAdded.includes(g.top)`**（那是上一版的错，新结构下「移出索引」按钮会永远不出现）。
- **P13 「加入索引」是隐私开放动作**：点了等于把这些笔记交给会话检索，已加二次确认；自测时**不要**真对 `40-Life`（44 个私密文件）点确认，验证到确认行出现再点「取消」即可。
- **P14 `indexed:false` 不是失败**：写进未解锁保护目录会 200 + 落盘成功，只是检索层拿不到。必须按 `indexHint()` 给提示，否则用户判定「存好了却问不到 = 你们写坏了」。
- **P15 响应 `provider` 可能与所选不同**：限流时后端自动换档一次（`zhipu`↔`glm4flash`、`deepseek`→`zhipu`）。标签以响应为准；`provider` 为 `null` 表示这条不是模型生成的（清空记忆确认语、两档全失败的兜底），此时不渲染模型标签。
- **P16 安全基线只有 3 条**（`.mimocode` / `node_modules` / `.obsidian`，实测 `GET /api/rag/patterns`）。`40-Life` 是**可解锁的保护目录**不是基线，两者文案与能力都不同——`RAG使用指南.md` 曾把 40-Life 写成基线第四条，已纠正。
- **P17 状态灯「在线」不等于管理面可用**：`/api/health` 免令牌，后端把服务只绑 `127.0.0.1:8080`、管理面全部加门禁之后，health 依旧返回 ok。判断管理面可用性要用 `/api/rag/status` 的 401/200（现有 `openRagCard` 就是这么做的）。
- **P18 「新建会话」会把模型硬回 GLM-4.7**：`newSession()` 里直接把 `model` 置为 `zhipu`，这是刻意的（新会话默认走免费档，省钱），不是 §10-P1 复发。偏好持久化本身已正常，它只作用于**打开已有会话**时的回填；要改这条行为先动 D5。
- **P19 隐私模型分两层，文案别混**：**文件可见性**由令牌管（持令牌 = 全库文件名可见，这层遮挡已按需求取消）；**是否进检索**由索引开关与排除规则管。写成「受保护 = 看不见」就是把两层混成一谈，用户会以为加了索引也不会被读到。

---

## 11. 与后端对接的信息位置

- 后端代码：`C:\Users\ChelseaFC\Desktop\12Java\study\Spring-AI`（GitHub: `ALDOcoder/wechat-ai-bot`），本文只描述前端视角的契约，后端行为以后端仓库当前代码为准。
- 后端接口/需求文档：`Spring-AI/agent/<模型>/back/` 下，命名带日期戳。本文这轮依据的是 `agent/qwen/back/vault-tree-visibility-and-note-dir-frist-20260921-2131.md`（文件树全可见 + `indexed` 字段 + 按目录写入）。**先读对应日期的接口文档，再读 §4.1 表**；两者冲突时以接口文档 + 后端实测为准，并把差异回写进本文。
- 密钥与库路径是**用户级注册表环境变量**（`RAG_VAULT_KEY` / `OBSIDIAN_VAULT_PATH`），新开终端读不到；取值：
  `powershell.exe -NoProfile -Command "[Environment]::GetEnvironmentVariable('RAG_VAULT_KEY','User')"`（只打印长度，别回显明文）。
- 日常使用视角的说明在 `RAG使用指南.md`；本轮之前的演进过程在 `开发总结.md`。
