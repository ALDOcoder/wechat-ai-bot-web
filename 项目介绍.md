# 本地知识助手 · wechat-ai-bot-web

> 一个基于 **Vue 3 + Vite** 的轻量级 AI 聊天面板，为 Java 后端 [wechat-ai-bot](https://github.com/ALDOcoder/wechat-ai-bot) 提供 Web 端交互界面。支持**多会话管理**、**三模型切换（免费 / 备用 / 付费）**、**本地知识库 RAG 检索**与**对话记忆控制**，开箱即用。

`Vue 3.5` · `Vite 6` · `零 UI 框架依赖` · `前后端分离`

---

## 📖 项目简介

**wechat-ai-bot-web** 是 `wechat-ai-bot` 项目的 Web 前端，与后端服务（Spring Boot，默认端口 `8080`）完全拆分、独立部署。它把原本只在微信端可用的 AI 机器人能力搬到浏览器中，提供一个类 ChatGPT 风格的对话界面：

- 🗂️ **左侧会话栏**：多会话并行，随时新建、切换、删除
- 💬 **右侧对话区**：气泡式消息流，实时状态反馈
- 🔌 **与后端通过 `/api` 通信**：开发期由 Vite 代理转发，生产期可由 Nginx 反代

前端仅依赖一个运行时包（`vue`），其余全部为原生实现——**没有引入任何 UI 组件库和状态管理库**，整体构建产物极小，适合作为学习 Vue 3 组合式 API 或快速搭建自有 AI 面板的起点。

---

## 🏗️ 架构总览

```
┌─────────────────────────────┐         ┌──────────────────────────────┐
│      wechat-ai-bot-web      │  /api   │       wechat-ai-bot          │
│    (Vue 3 + Vite, :5173)    │ ──────▶ │   (Spring Boot, :8080)       │
│                             │  proxy  │                              │
│  会话列表 / 消息气泡 / 设置   │         │  GLM-4.7-Flash（免费，默认）  │
│  模型切换 / RAG 开关 / 状态灯 │         │  GLM-4-Flash（免费备选）      │
└─────────────────────────────┘         │  DeepSeek（付费，需授权）     │         │  本地 Obsidian 笔记 RAG       │
                                        │  message_log 会话落库         │
                                        └──────────────────────────────┘
```

**通信约定**

- 开发环境：Vite 将 `/api` 代理到 `http://127.0.0.1:8080`，开启 `changeOrigin` + `xfwd`（转发客户端真实 IP）
- 所有请求携带 `scene: 'web'`，会话 ID 以 `web:<uuid>` 标识，后端按客户端 IP 区分用户并将消息落库到 `message_log`
- 模型偏好通过 `PUT /api/conversations/:id/setting` 持久化到后端，刷新页面、换设备后依然生效

---

## ✨ 核心功能

### 1. 多会话管理
- 一键新建会话（UUID 标识），支持会话间的自由切换与删除
- 会话卡片展示**标题、最近活跃时间（刚刚 / x 分钟前 / x 小时前 / 日期）、消息条数**
- 会话历史由后端持久化，刷新页面自动恢复上次的会话与消息记录

### 2. 三模型切换：免费 + 备用 + 付费
| 模型 | 说明 | 费用 |
| --- | --- | --- |
| **GLM-4.7**（默认） | 智谱 GLM-4.7-Flash | 免费 |
| **GLM-4** | 智谱 GLM-4-Flash，主力模型限流（429）时的免费备选 | 免费 |
| **DeepSeek** | deepseek-chat | 按量付费，需授权 |

- 输入舱内下拉选择，当前选择即时写回后端持久化，默认 GLM-4.7
- 每条 AI 回复附带**来源标签**（⚡ DeepSeek / GLM-4.7 / GLM-4），生成成本一目了然
- **智能回落**：选择 DeepSeek 但未授权或服务不可用时，后端自动回落到免费模型，前端显示「回落免费」提示，对话永不中断

### 3. 本地知识库 RAG 检索
- 开关式控制：开启后提问会检索本地 **Obsidian 笔记**，让 AI 基于「你自己的知识」回答
- 命中知识库的回复会打上 `📄 RAG` 标签，可追溯回答依据
- **知识库管理台**（侧栏入口，密钥门禁）：管理面整体加密，锁屏输入密钥换取临时令牌后可使用：
  - **文件树**：浏览全库 md 路径，三态标记（🟢 可检索 / ⚪ 已排除 / 🔒 受保护）
  - **受保护目录**：`40-Life` 等目录默认不索引且隐藏子文件，可加入/移出索引（持久化、可逆）
  - **手动新建笔记**：指定路径 + Markdown 内容直接入库
  - **排除规则**：基线只读 + 自定义规则增删改启停，变更自动重建索引
- **AI 写入确认**：AI 请求写笔记时聊天流弹出确认卡片（路径 + 内容预览），确认后才落盘进索引，拒绝则丢弃

### 4. 对话记忆控制
- 「清空记忆」一键重置当前会话的上下文，AI 立刻"失忆"重新开始

### 5. 贴心的交互细节
- 🟢 **服务健康检查**：进入页面自动探测 `/api/health`，顶栏状态灯实时显示「服务在线 / 离线 / 检测中」
- ⏳ **打字等待动画**：发送后气泡内三点跳动，消除等待焦虑
- ⌨️ **键盘友好**：`Enter` 发送、`Shift+Enter` 换行
- 📜 **自动滚动**：新消息始终保持在视野内
- 🛡️ **优雅降级**：后端未启动时给出明确的中文错误指引，而非白屏或报错堆栈

---

## 🛠️ 快速开始

### 环境要求
- Node.js ≥ 18
- 后端 [wechat-ai-bot](https://github.com/ALDOcoder/wechat-ai-bot) 已在 `8080` 端口运行

### 启动

```bash
# 1. 安装依赖
npm install

# 2. 启动开发服务器（默认 http://127.0.0.1:5173）
npm run dev

# 3. 生产构建 / 本地预览构建产物
npm run build
npm run preview
```

打开浏览器访问 **http://127.0.0.1:5173**，看到「服务在线」状态灯亮起即可开始对话。

---

## 📡 后端 API 依赖清单

前端与后端通过以下接口协作，便于对接自有后端：

| 方法 | 路径 | 用途 |
| --- | --- | --- |
| `GET` | `/api/health` | 健康检查，返回 `{ status: 'ok' }` |
| `GET` | `/api/sessions` | 会话列表（标题 / 最后活跃时间 / 消息数） |
| `GET` | `/api/sessions/:id/messages` | 拉取指定会话的历史消息 |
| `DELETE` | `/api/sessions/:id` | 删除指定会话 |
| `GET` / `PUT` | `/api/conversations/:id/setting` | 读取 / 持久化模型偏好与 DeepSeek 授权 |
| `POST` | `/api/reply` | 发送消息并获取 AI 回复 |
| `GET` | `/api/rag/status` | RAG 索引状态（启用 / 文件数 / 分块数 / 库路径） |
| `GET` / `POST` | `/api/rag/patterns` | 排除规则列表 / 新增规则（自动重建索引） |
| `PUT` / `DELETE` | `/api/rag/patterns/:id` | 启停 / 删除排除规则 |
| `POST` | `/api/vault/session` | 管理面密钥换临时令牌（`X-Vault-Token`） |
| `GET` | `/api/vault/tree` | 文件树（三态，安全基线不出现） |
| `POST` | `/api/vault/protect/add` / `remove` | 受保护目录加入 / 移出索引 |
| `POST` | `/api/vault/note` | 手动新建笔记 |
| `POST` | `/api/vault/note/confirm` / `reject` | 确认 / 拒绝 AI 写入草稿 |

`POST /api/reply` 请求体示例：

```json
{
  "sender": "web",
  "content": "帮我总结一下今天的笔记",
  "useRag": true,
  "scene": "web",
  "sessionId": "3f2a…-uuid",
  "provider": "deepseek"
}
```

响应中的 `provider`、`ragUsed` 字段会被前端渲染为来源标签，`ragUsed` 为 `true` 时显示 📄 RAG 标记。

---

## 📁 项目结构

```
wechat-ai-bot-web
├── index.html              # 入口 HTML（中文标题「本地知识助手」）
├── package.json            # 仅 1 个运行时依赖：vue
├── vite.config.js          # 端口 5173 + /api 代理（changeOrigin + xfwd）
└── src
    ├── main.js             # 应用挂载入口
    ├── App.vue             # 聊天面板全部逻辑（约 350 行单文件组件）
    └── styles.css          # 纯手写样式，无 UI 框架
```

---

## 🎨 设计说明

- **DeepSeek 官网式布局**：纯白全屏 + 浅灰侧栏 + DeepSeek 蓝（`#4d6bfe`）主色，欢迎页为居中的「随时开始吧」+ 大圆角输入舱，无多余顶栏
- **功能即胶囊**：模型下拉选择（默认 GLM-4.7）与「知识库检索」开关以胶囊形式内嵌在输入舱左下，随手可切
- **会话按时间分组**：今天 / 7 天内 / 30 天内 / 更早，侧栏「开启新对话」胶囊按钮一键新建
- **移动端完整适配**：≤768px 时侧栏变抽屉（汉堡按钮 + 遮罩）、顶栏压缩、气泡放宽、输入舱适配 iPhone 底部安全区
- **信息即标签**：模型来源、RAG 命中、费用回落全部以小标签内联在气泡中，无需跳转查看
- **零依赖哲学**：不用 Element Plus / Pinia / Axios，`fetch` + Vue 响应式即可覆盖全部需求，代码量小、可读性高，非常适合二次开发

---

## 🔭 可扩展方向

- [ ] 回复内容 Markdown 渲染与代码高亮
- [ ] 接入 SSE / WebSocket 实现**流式输出**（逐字打印）
- [ ] 用户登录体系，替代当前「按 IP 区分会话」的轻量方案
- [ ] 会话搜索与导出（Markdown / JSON）
- [ ] Docker 一键部署（前端 Nginx 反代 + 后端 Java 服务）

---

## 🔗 相关仓库

- 后端服务（Java + Spring Boot + 微信接入 + RAG）：[ALDOcoder/wechat-ai-bot](https://github.com/ALDOcoder/wechat-ai-bot)
